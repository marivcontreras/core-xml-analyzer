"""
Radvd Validation Configuration

Consolidates all hardcoded parameters used in radvd validation (radvd.py).
Modify these constants to adjust radvd validation behavior without changing code logic.

To adjust validation rules:
1. Modify the constants below
2. No changes needed to validation logic (radvd.py)
3. Run tests to verify behavior

NOTE: This module is currently a placeholder. After auditing radvd.py for hardcoded
values, configuration parameters will be extracted here.
"""

# Add radvd-specific configuration here after audit phase
# Example format (will be populated during audit):
# RADVD_MIN_PREFIX_LENGTH = 64
